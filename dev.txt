================================================================================
  WEB-VULN-CONSOLE — DEV TRACKER v4
  Project: Web Vulnerability Scanner Console
  Repo: xbox360badupdateexploitAIOstealth/web-vuln-console
  Last Updated: June 6, 2026
  Status: Active Development — Phase 2/3

  AUDIT METHOD: All TODOs verified against commit history (30 commits read).
  Engine, registry, and check file states reflect actual code as of latest commit.
================================================================================

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SECTION 1: COMPLETE COMMIT HISTORY (oldest → newest)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Early setup commits (exact SHAs not tracked in history view):
    feat(task-4):   projects page HTML structure
    feat(task-7):   reportGenerator.js — HTML + Markdown export
    feat(task-8):   server.js v2 — retry, global stats, PATCH, env field, error handler
    feat(engine):   engine-bridge.js v2 — TLS, cookie, API key leak, subdomain takeover,
                    JWT exposure, expanded SQLi/XSS payloads
    docs:           dev.txt v3 — fully verified by source read

  TODO-01  5ede63f  feat(checks): add cookieSession.js
  TODO-01  2afb6d5  feat(engine): wire Phase 1b cookieSession + Phase 2.5 jsSecretScan
  TODO-02  2dc9942  feat(checks): add jsSecretScan.js
  TODO-02  c692cc9  feat(registry): add exposure.js.secrets + cookie.session.flags ModuleDefs
  TODO-03  d42d334  feat(checks): add sourceMapDetect.js
  TODO-03  9f85c35  feat(checks): sourceMapDetect.js v2 — header detect, XSSI strip, dedup, path leak
  TODO-03  c3c44bf  feat(engine): wire Phase 2.5 sourceMapDetect
  TODO-03  9e40ac3  feat(registry): add exposure.sourcemap ModuleDef
  TODO-04  0cbbfe9  feat(todo-04): cPanelWhm.js + cidrExpand.js + engine + registry
  TODO-04  c2bc56f  feat(todo-04): wire exposure.cve.cpanel_whm into engine Phase 1d
  TODO-26  e308d75  feat(TODO-26): custom payload library — SQLite, REST API, SecLists import, UI
  CVE FP   5775735  feat: add cveFingerprints.js — 15 CVE fingerprint checks
  CVE FP   4c55810  feat(moduleRegistry): add cve.fingerprints ModuleDef
  CVE FP   fbb5bbf  feat(engine): wire Phase 1e cveFingerprints — v1.5.0 → v1.6.0
  CVE FP   ffe30e5  feat(policyRegistry): enable cve.fingerprints in normal + aggressive
  TODO-05  28af739  feat(todo-05): laravelEnv.js — advanced Laravel .env hunter [1/3]
  TODO-05  c7c94ff  feat(todo-05): add exposure.cve.laravel_env_hunt ModuleDef [2/3]
  TODO-05  015ea73  feat(todo-05): wire Phase 1f laravel_env_hunt into engine [3/3]
  docs     1dacd13  docs: update README with full commit log, todo tracker, engine state
  Phase2.5 33dc421  feat: add robotsTxtParse.js — passive robots.txt + sitemap.xml recon (Phase 2.5c)
  TODO-06  5bc5a41  feat(todo-06): add cvePassive.js — phpinfo, SVN/Hg, Vite, Mautic, Moodle,
                    cloud buckets, WP debug [1/3]
  TODO-06  ff4c087  feat(todo-06): add 7 ModuleDefs for cvePassive checks [2/3]
  siteModel 6bb0dd2 feat(siteModel): add techStack property + addDiscoveredPath()
  TODO-06  b637048  feat(todo-06): wire 7 cvePassive checks into engine Phase 1g [3/3]


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SECTION 2: VERIFIED FILE INVENTORY (current as of v4)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[ROOT]
  ✅ .gitignore
  ✅ README.md
  ✅ setup.sh
  ✅ dev.txt             (this file)

[FRONTEND — frontend/]
  ✅ frontend/index.html     (9.5 KB)  — dark terminal console UI shell
  ✅ frontend/style.css      (14 KB)   — cyberpunk terminal theme, responsive
  ✅ frontend/loader.css     (1.2 KB)  — loading animations
  ✅ frontend/app.js         (~50 KB)  — core SPA frontend logic (LARGEST FILE)
  ✅ frontend/report.js      — report generation UI module (integration unverified)

[PUBLIC — public/]
  ✅ public/index.html
  ✅ public/styles.css

[BACKEND — backend/]
  ✅ backend/server.js           (16.1 KB)  — Express HTTP API v2.0.0
  ✅ backend/config.js           (715 B)
  ✅ backend/db.js               (17.5 KB)  — SQLite via better-sqlite3
  ✅ backend/jobsStore.js        (1.7 KB)   — ⚠️ IN-MEMORY ONLY (see TODO-08)
  ✅ backend/worker.js           (2.2 KB)   — background job worker
  ✅ backend/engine-bridge.js    (44.1 KB)  — standalone scan engine (NOT using src/core/ yet — TODO-14)
  ✅ backend/dorkEngine.js       (9.5 KB)   — Google + GitHub dork generator
  ✅ backend/reportGenerator.js  (11.1 KB)  — HTML + Markdown report builder
  ✅ backend/package.json
  ✅ backend/README.md
  ✅ backend/data/               — empty placeholder dir for scanner.db
  ✅ backend/middleware/rateLimiter.js    (1.1 KB)
  ✅ backend/middleware/requestLogger.js  (618 B)
  ✅ backend/routes/dorksRoute.js         (430 B)   — GET /api/dorks
  ✅ backend/routes/reportsRoute.js       (872 B)   — GET /api/scans/:jobId/report
  ✅ backend/routes/payloadsRoute.js      — full CRUD + SecLists import (TODO-26 ✅)
  ✅ backend/utils/normalize.js           (1.3 KB)
  ✅ backend/utils/retry.js               (1.0 KB)
  ✅ backend/utils/severityScore.js       (1.4 KB)
  ✅ backend/utils/cidrExpand.js          — CIDR → IP list expander (added TODO-04)

[SRC/CORE — src/core/]
  ✅ src/core/engine.js          — v1.8.0 (TODO-06 complete)
  ✅ src/core/crawler.js         (9.5 KB)
  ✅ src/core/httpClient.js      (1.3 KB)
  ✅ src/core/injection.js       (12.6 KB)
  ✅ src/core/models.js          (5.7 KB)
  ✅ src/core/moduleRegistry.js  — 23 ModuleDefs (as of TODO-06)
  ✅ src/core/policyRegistry.js  — 3 policies
  ✅ src/core/siteModel.js       — updated: techStack property + addDiscoveredPath()

  src/core/checks/
  ✅ checks/tlsHeaders.js        (19.8 KB)  — TLS + 6 security headers
  ✅ checks/cookieSession.js     — TODO-01 ✅ — HttpOnly/Secure/SameSite/entropy/URL token
  ✅ checks/jsSecretScan.js      — TODO-02 ✅ — AWS/Stripe/Google/Firebase/GitHub/JWT/Bearer
  ✅ checks/sourceMapDetect.js   — TODO-03 ✅ — .map probe, X-SourceMap header, sourceMappingURL,
                                              XSSI strip, dedup, internal path leak detection
  ✅ checks/cPanelWhm.js         — TODO-04 ✅ — CVE-2026-41940, ports 2082/2083/2086/2087/8080/8443
  ✅ checks/cveFingerprints.js   — Phase 1e ✅ — 15 CVE fingerprints (2025–2026)
  ✅ checks/laravelEnv.js        — TODO-05 ✅ — 56 paths, 50+ secret patterns, APP_KEY RCE annotation
  ✅ checks/robotsTxtParse.js    — Phase 2.5c ✅ — robots.txt + sitemap.xml passive recon
  ✅ checks/cvePassive.js        — TODO-06 ✅ — 7 checks (phpinfo, SVN/Hg, Vite @fs LFI, Mautic,
                                              Moodle, cloud buckets, WP debug artifacts)

[SRC/UI — src/ui/]
  ✅ src/ui/app.js
  ✅ src/ui/state.js
  ✅ src/ui/tos.js
  ✅ src/ui/views/findingsListView.js   — real table w/ status/severity
  ✅ src/ui/views/jobConsoleView.js     — live log view
  ✅ src/ui/views/projectListView.js    — project list
  ⚠️ src/ui/views/dashboardView.js     — SKELETON ONLY — no real API calls (TODO-09)
  ❌ src/ui/views/targetsView.js        — MISSING (TODO-12)
  ❌ src/ui/views/policyView.js         — MISSING (TODO-13)
  ❌ src/ui/views/settingsView.js       — MISSING (TODO-32)
  ❌ src/ui/views/modulesView.js        — MISSING (TODO-33)
  ❌ src/ui/views/projectDetailView.js  — MISSING (TODO-34)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SECTION 3: VERIFIED API ROUTES (server.js v2.0.0)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  CORE:
  ✅ GET    /api/health
  ✅ GET    /api/stats               — global totals: projects/targets/jobs/findings+severity
  ✅ GET    /api/audit

  PROJECTS:
  ✅ GET    /api/projects
  ✅ POST   /api/projects
  ✅ GET    /api/projects/:id
  ✅ PUT    /api/projects/:id
  ✅ PATCH  /api/projects/:id
  ✅ DELETE /api/projects/:id
  ✅ GET    /api/projects/:id/targets
  ✅ POST   /api/projects/:id/targets
  ✅ POST   /api/projects/:id/targets/bulk
  ✅ GET    /api/projects/:id/findings  — ?severity ?status ?category ?limit ?offset

  TARGETS:
  ✅ DELETE /api/targets/:targetId

  SCANS:
  ✅ POST   /api/scans               — queue scan (policyId, targets[])
  ✅ GET    /api/scans               — list all (?projectId filter)
  ✅ GET    /api/scans/:jobId
  ✅ GET    /api/scans/:jobId/results
  ✅ POST   /api/scans/:jobId/cancel
  ✅ POST   /api/scans/:jobId/retry
  ✅ GET    /api/scans/:jobId/report  — via reportsRoute

  FINDINGS:
  ✅ PUT    /api/findings/:id/status

  DORKS:
  ✅ GET    /api/dorks               — via dorksRoute

  PAYLOAD LIBRARY (TODO-26 ✅):
  ✅ GET    /api/payloads            — list all (?category ?tag ?search)
  ✅ POST   /api/payloads            — create
  ✅ PUT    /api/payloads/:id        — update
  ✅ DELETE /api/payloads/:id        — delete
  ✅ POST   /api/payloads/import     — bulk import (SecLists-compatible format)
  ✅ GET    /api/payloads/categories — list distinct categories


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SECTION 4: MODULE REGISTRY (23 ModuleDefs as of TODO-06)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  PASSIVE — EXPOSURE (core):
  ✅ exposure.env.direct            /.env direct check
  ✅ exposure.env.variants          8 variant paths (.env.local/.bak/.backup/etc)
  ✅ exposure.backup.db_dumps       /backup.sql, /db.sql, /dump.sql, etc.
  ✅ exposure.backup.archives       .zip, .tar.gz backup archives
  ✅ misconfig.dirlisting.generic   Index of / detection
  ✅ vcs.git.exposed                /.git/HEAD, /.git/config
  ✅ debug.stacktraces              Stack trace / verbose error leakage

  PASSIVE — TLS & HEADERS:
  ✅ tls.headers.basic              HSTS/CSP/X-Content-Type/X-Frame/Perms/Referrer

  PASSIVE — COOKIE & SESSION (TODO-01):
  ✅ cookie.session.flags           HttpOnly, Secure, SameSite, entropy, URL token

  PASSIVE — JS ANALYSIS (TODO-02, TODO-03):
  ✅ exposure.js.secrets            AWS/Stripe/Google/Firebase/GitHub/JWT/Bearer secret scan
  ✅ exposure.sourcemap             .map probe + X-SourceMap header + sourceMappingURL

  PASSIVE — CVE / PRODUCT CHECKS (TODO-04, TODO-05, Phase 1e, TODO-06):
  ✅ exposure.cve.cpanel_whm        CVE-2026-41940 cPanel/WHM port scan
  ✅ cve.fingerprints               15 CVE fingerprints 2025–2026
  ✅ exposure.cve.laravel_env_hunt  56 paths, 50+ secrets, APP_KEY RCE annotation
  ✅ misconfig.phpinfo.exposed      /phpinfo.php, /info.php, Symfony profiler, Apache status
  ✅ vcs.svn_hg.exposed             /.svn/entries, /.svn/wc.db, /.hg/requires
  ✅ exposure.cve.vite_bypass       Vite @fs LFI (CVE-2025-30208 / CVE-2025-46565)
  ✅ exposure.cve.mautic_env        Mautic .env (CVE-2024-47056)
  ✅ exposure.cve.moodle_listing    Moodle r.php + data dir listing (CVE-2025-62396)
  ✅ exposure.cloud.open_bucket     S3 / Azure Blob / GCS open bucket probe
  ✅ exposure.cms.wp_debug          WP debug.log + wp-config.php.bak + vim swap files

  ACTIVE — INJECTION:
  ✅ injection.sqli.basic           6 payloads, 12 DB error signatures
  ✅ injection.xss.reflected_basic  4 payloads, raw reflection check
  ✅ injection.path_traversal.basic Linux + Windows traversal, file signatures


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SECTION 5: ENGINE WIRING (engine.js v1.8.0)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Phase 1   — runPassiveExposureChecks()
    ✅ exposure.env.direct, exposure.env.variants, exposure.backup.db_dumps,
      exposure.backup.archives, misconfig.dirlisting.generic,
      vcs.git.exposed, debug.stacktraces

  Phase 1b  — runTlsHeaderChecks()          (checks/tlsHeaders.js)
    ✅ tls.headers.basic

  Phase 1c  — runCookieSessionChecks()      (checks/cookieSession.js) [TODO-01]
    ✅ cookie.session.flags

  Phase 1d  — runCPanelWhmScan()            (checks/cPanelWhm.js) [TODO-04]
    ✅ exposure.cve.cpanel_whm

  Phase 1e  — runCveFingerprints()          (checks/cveFingerprints.js)
    ✅ cve.fingerprints (15 CVEs)

  Phase 1f  — runLaravelEnvHunt()           (checks/laravelEnv.js) [TODO-05]
    ✅ exposure.cve.laravel_env_hunt

  Phase 1g  — cvePassive suite             (checks/cvePassive.js) [TODO-06]
    ✅ misconfig.phpinfo.exposed
    ✅ vcs.svn_hg.exposed
    ✅ exposure.cve.vite_bypass
    ✅ exposure.cve.mautic_env
    ✅ exposure.cve.moodle_listing
    ✅ exposure.cloud.open_bucket
    ✅ exposure.cms.wp_debug

  Phase 2   — crawlTargetAndBuildSiteModel() (crawler.js)
    ✅ maxDepth: 2, maxPages: 30
    ✅ SiteModel: endpoints, parameterized URLs, JS asset URLs
    ✅ siteModel.techStack + addDiscoveredPath() now available

  Phase 2.5a — runJsSecretScan()           (checks/jsSecretScan.js) [TODO-02]
    ✅ exposure.js.secrets

  Phase 2.5b — runSourceMapDetect()        (checks/sourceMapDetect.js) [TODO-03]
    ✅ exposure.sourcemap

  Phase 2.5c — robotsTxtParse             (checks/robotsTxtParse.js)
    ✅ robots.txt + sitemap.xml passive recon
    ⚠️ NOTE: robotsTxtParse.js exists and is written but engine wiring
           status needs verification — confirm it is called in scanTarget()

  Phase 3   — runActiveInjectionChecks()   (injection.js)
    ✅ injection.sqli.basic
    ✅ injection.xss.reflected_basic
    ✅ injection.path_traversal.basic
    ❌ injection.cmdi.basic        — NOT YET (TODO-07)
    ❌ injection.ssti.basic        — NOT YET (TODO-07)
    ❌ injection.fileupload.detect — NOT YET (TODO-07)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SECTION 6: SCAN POLICIES (policyRegistry.js)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  policy_normal     — all passive modules enabled (no injection)
                      includes: tls.headers.basic, cookie.session.flags,
                      exposure.js.secrets, exposure.sourcemap,
                      exposure.cve.cpanel_whm, cve.fingerprints,
                      exposure.cve.laravel_env_hunt, all TODO-06 modules

  policy_aggressive — normal + SQLi + XSS
                      adds: injection.sqli.basic, injection.xss.reflected_basic

  policy_extreme    — aggressive + path traversal
                      adds: injection.path_traversal.basic
                      ⚠️ TODO-07 modules (cmdi, ssti, fileupload) not yet added
                      ⚠️ TODO-06 policy entries need verification that
                         all 7 cvePassive modules are enabled in policy_extreme


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SECTION 7: KNOWN GAPS & ACTIVE WARNINGS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ⚠️ TWO PARALLEL SCAN ENGINES (critical architecture gap — TODO-14)
     backend/engine-bridge.js (44 KB) is its own complete scan system.
     src/core/engine.js is the modular engine (correct architecture).
     They are completely independent. Fix: engine-bridge.js should become
     a thin adapter importing runScanJob from src/core/engine.js.
     STATUS: NOT FIXED. Do last (requires full testing after Tier 1–5 complete).

  ⚠️ JOBS NOT PERSISTED (TODO-08)
     backend/jobsStore.js is pure in-memory Map.
     Server restart = all job metadata lost.
     Findings ARE saved to SQLite (db.js) but job records are not.

  ⚠️ dashboardView.js IS A SKELETON (TODO-09)
     633 bytes, zero API calls, no real data rendered.
     GET /api/stats exists and works. Just needs wiring.

  ⚠️ 5 UI VIEWS MISSING (TODO-12/13/32/33/34)
     targetsView.js, policyView.js, settingsView.js,
     modulesView.js, projectDetailView.js — none exist.

  ⚠️ frontend/report.js INTEGRATION UNVERIFIED
     File exists but not confirmed imported in app.js.

  ⚠️ robotsTxtParse.js ENGINE WIRING UNVERIFIED
     File exists (Phase 2.5c). Confirm it is called inside scanTarget().

  ⚠️ policyRegistry.js TODO-06 COVERAGE UNVERIFIED
     All 7 cvePassive module IDs need to be confirmed enabled in
     policy_normal, policy_aggressive, and policy_extreme.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SECTION 8: PRIORITY TODO LIST — ORDERED (updated June 6 2026)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ✔️ DONE:
  TODO-01  cookieSession.js — COMPLETE
  TODO-02  jsSecretScan.js — COMPLETE
  TODO-03  sourceMapDetect.js v2 — COMPLETE
  TODO-04  cPanelWhm.js + cidrExpand.js — COMPLETE
  TODO-05  laravelEnv.js — COMPLETE
  TODO-06  cvePassive.js (7 checks) — COMPLETE
  TODO-26  Payload library (SQLite + REST API + SecLists import + UI) — COMPLETE

  ── TIER 3: INJECTION.JS ADDITIONS ────────────────────────────────────────────

  TODO-07  injection.js additions  [EDIT src/core/injection.js]
           Add runCommandInjection():
             Payloads: ; id, & whoami, | id, `id`, $(id)
             Detection: uid= / root / Windows username in response
           Add runSstiChecks():
             Payloads: {{7*7}}, ${7*7}, #{7*7}, <%= 7*7 %>
             Detection: 49 reflected, or template engine error string
           Add runFileUploadDetect():
             Discover upload forms from SiteModel
             Probe: benign .txt, then .php/.jsp extension test
             Detection: 200 + filename reflected, or extension error
           Add ModuleDefs: injection.cmdi.basic, injection.ssti.basic,
                           injection.fileupload.detect
           Update policyRegistry.js: policy_extreme enables all three
           Touches: injection.js, moduleRegistry.js, policyRegistry.js

  ── TIER 4: BACKEND FIXES ──────────────────────────────────────────────────

  TODO-08  Persist jobs to DB  [EDIT backend/db.js + backend/jobsStore.js]
           Add jobs table: id, project_id, status, policy_id, targets_json,
           description, created_at, started_at, finished_at, result_json
           Update jobsStore.js to read/write SQLite
           Fixes: job metadata lost on server restart

  ── TIER 5: FRONTEND / UI ─────────────────────────────────────────────────

  TODO-09  Dashboard wired to real data  [EDIT src/ui/views/dashboardView.js]
           GET /api/stats → stats ring chart, recent jobs, recent critical findings

  TODO-10  Report generation UI  [EDIT frontend/app.js + verify report.js]
           Wire “Generate Report” → GET /api/scans/:jobId/report
           Show download link for HTML + Markdown
           Verify frontend/report.js imported in app.js

  TODO-11  Finding detail side-panel  [EDIT findingsListView.js]
           On row click: slide-in with URL, evidence snippet, OWASP/CWE,
           remediation guidance, status changer

  TODO-12  Targets management view  [NEW src/ui/views/targetsView.js]
           Per-project: list + add/edit/delete, bulk paste import,
           env tag (prod/staging/dev), IP range input (for TODO-04)

  TODO-13  Policy editor UI  [NEW src/ui/views/policyView.js]
           Module toggle checkboxes per policy

  ── TIER 6: ARCHITECTURE (DO LAST) ───────────────────────────────────────

  TODO-14  engine-bridge.js → src/core/engine.js unification
           engine-bridge.js becomes thin adapter (~3 KB)
           src/core/ becomes single source of truth for ALL scan logic
           ⚠️ BIGGEST STRUCTURAL CHANGE — do after all Tier 1–5 complete

  ── TIER 7: NICE-TO-HAVE ──────────────────────────────────────────────────

  TODO-15  WebSocket live job feed (replace polling)
  TODO-16  Scan diff mode (NEW/FIXED/UNCHANGED per finding vs previous scan)
  TODO-17  Subdomain enumeration (crt.sh Certificate Transparency + DNS wordlist)
  TODO-18  Scheduled scans (daily/weekly cron)
  TODO-19  Slack/Discord webhook on critical finding
  TODO-20  Screenshot capture on critical findings (headless browser)
  TODO-21  CLI client (scanner add-target, scanner run-scan)
  TODO-22  Mobile PWA (installable Android/iOS)
  TODO-23  LLM-assisted finding summarization
  TODO-24  User auth + role system (owner/analyst/view-only)
  TODO-25  Evidence Vault (encrypted storage, project lock)
  TODO-26  ✔️ COMPLETE — Custom payload library (SQLite + REST + SecLists import + UI)
  TODO-27  OWASP Top 10 2025 coverage checklist per project
  TODO-28  Target import from .txt/.csv file
  TODO-29  Quick-launch scan templates (WordPress Full Audit, Laravel Env Hunt,
           cPanel Range Sweep, Vite Dev Server Hunt)
  TODO-30  Dork assistant panel (in-app clickable dorks)
  TODO-31  REST API documentation page
  TODO-32  settingsView.js — API keys, scan defaults, concurrency settings
  TODO-33  modulesView.js — browsable module catalog
  TODO-34  projectDetailView.js — single project full summary view
  TODO-35  Per-project “playbook” mode (sequence of scan types saved as template)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SECTION 9: ARCHITECTURE NOTES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  TWO PARALLEL SCAN CODE TREES (must be reconciled — TODO-14):

    /src/core/     — modular engine (CORRECT architecture going forward)
                     engine.js v1.8.0, injection.js, crawler.js, siteModel.js,
                     moduleRegistry.js (23 ModuleDefs), policyRegistry.js,
                     checks/ (9 check files + robotsTxtParse)

    /backend/      — Express API + engine-bridge.js (its own scan logic)
                     engine-bridge.js (44 KB) runs scans independently.
                     Does NOT import src/core/engine.js at all.

  SCAN POLICY IDs (policyRegistry.js — 3 policies):
    policy_normal     — passive only
    policy_aggressive — passive + SQLi + XSS
    policy_extreme    — ALL modules (will grow as Tier 1–3 TODOs complete)

  DB FILE:
    backend/data/scanner.db (auto-created by better-sqlite3 on first run)
    .gitignore excludes scanner.db from commits

  DEPLOYMENT:
    Node backend port 7777 (Termux / VPS)
    Frontend served as static files by Express from /frontend
    Browser-only mode: limited by CORS — backend agent recommended for real scans

  CIDR / IP RANGE INPUT (added TODO-04):
    backend/utils/cidrExpand.js — CIDR → IP list expander
    Used by cPanelWhm.js for multi-IP sweeps.
    Targets currently take single host string; CIDR feeds bulk target creation.

  PAYLOAD LIBRARY (added TODO-26):
    SQLite table: payloads (id, name, category, tags, content, source, created_at)
    Full CRUD REST API at /api/payloads
    POST /api/payloads/import supports SecLists-format bulk import
    UI: browsable, searchable payload panel (integration status unverified in app.js)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  END — dev.txt v4 — web-vuln-console
  Source-verified June 6 2026. Always authorized. Always documented.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
