================================================================================
  WEB-VULN-CONSOLE — DEV TRACKER & FEATURE STATUS
  Project: Web Vulnerability Scanner Console
  Repo: xbox360badupdateexploitAIOstealth/web-vuln-console
  Last Updated: June 5, 2026
  Status: Active Development — Phase 2/3
  NOTE: This file is ground-truth. Every entry verified by full repo audit.
================================================================================

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SECTION 1: WHAT HAS BEEN CODED & IS WORKING ✅
  (Verified by direct file read — June 5 2026)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[FRONTEND SHELL - WORKING]
  ✅ frontend/index.html     — Full dark-terminal console UI shell (9.5 KB)
  ✅ frontend/style.css      — Dark cyberpunk terminal theme, fully responsive (14 KB)
  ✅ frontend/loader.css     — Loading animations and spinners (1.2 KB)
  ✅ frontend/app.js         — ~50 KB core SPA frontend logic (LARGEST FILE)
  ✅ public/index.html       — Entry point / redirect shell
  ✅ public/styles.css       — Base styles

[UI VIEWS - WORKING]
  ✅ src/ui/app.js               — UI bootstrap and router (1.4 KB)
  ✅ src/ui/state.js             — Global UI state store (239 B)
  ✅ src/ui/tos.js               — ToS modal + authorization gate (3.2 KB) — REQUIRED before scanning
  ✅ src/ui/views/findingsListView.js  — Findings table with status/severity display (5.1 KB)
  ✅ src/ui/views/jobConsoleView.js    — Live job log console view (3.2 KB)
  ✅ src/ui/views/projectListView.js   — Project list and management view (1.7 KB)
  ⚠️ src/ui/views/dashboardView.js     — EXISTS but is skeleton only (633 B) — no real data wired

[BACKEND SERVER - WORKING]
  ✅ backend/server.js           — Express.js HTTP server v1.1.0 (12 KB)
  ✅ backend/config.js           — Server config constants (715 B)
  ✅ backend/db.js               — SQLite persistence layer via better-sqlite3 (17.5 KB) — FULLY BUILT
                                    Includes: projects, targets, findings, evidence, auditLog CRUD
                                    Cascading deletes, severity summary, bulk target insert all done
  ✅ backend/jobsStore.js        — In-memory job queue + tracking (1.7 KB)
  ✅ backend/worker.js           — Background worker thread for scan jobs (2.2 KB)
  ✅ backend/engine-bridge.js    — Core backend scan bridge (27 KB) — LARGE, duplicates some engine logic
  ✅ backend/dorkEngine.js       — Google + GitHub dork generator (9.5 KB)
  ✅ backend/reportGenerator.js  — HTML + Markdown report builder (11 KB)
  ✅ backend/middleware/rateLimiter.js   — Per-IP rate limiter (1.1 KB)
  ✅ backend/middleware/requestLogger.js — Request logging middleware (618 B)
  ✅ backend/routes/dorksRoute.js        — /api/dorks endpoint (430 B)
  ✅ backend/routes/reportsRoute.js      — /api/reports endpoint (872 B)
  ✅ backend/utils/normalize.js   — URL/domain normalizer (1.3 KB)
  ✅ backend/utils/retry.js       — HTTP retry with exponential backoff (1 KB)
  ✅ backend/utils/severityScore.js — CVSS-style severity scoring (1.4 KB)

[BACKEND API ROUTES - WORKING]
  ✅ GET  /api/health
  ✅ GET  /api/projects                       — list all projects with stats
  ✅ POST /api/projects                       — create project
  ✅ GET  /api/projects/:id                   — get one project
  ✅ PUT  /api/projects/:id                   — update project
  ✅ DELETE /api/projects/:id                 — delete project (cascades)
  ✅ GET  /api/projects/:id/targets           — list targets for project
  ✅ POST /api/projects/:id/targets           — add one target
  ✅ POST /api/projects/:id/targets/bulk      — bulk add targets from array
  ✅ DELETE /api/targets/:targetId            — remove a target
  ✅ POST /api/scans                          — start a scan job
  ✅ GET  /api/scans                          — list jobs
  ┅ GET  /api/scans/:jobId                   — get job status
  ✅ GET  /api/scans/:jobId/results           — get job results
  ✅ POST /api/scans/:jobId/cancel            — cancel a job
  ✅ GET  /api/projects/:id/findings          — findings list with filters
  ✅ PUT  /api/findings/:id/status            — update finding status
  ✅ GET  /api/audit                          — recent audit log
  ✅ GET  /api/dorks                          — dork generator

[CORE ENGINE - WORKING]
  ✅ src/core/engine.js         — Main scan orchestrator v1.2.0 (16 KB)
                                  Phase 1:  Passive exposure checks → LIVE
                                  Phase 1b: TLS + security headers → LIVE
                                  Phase 2:  Crawler → builds SiteModel → LIVE
                                  Phase 3:  Active injection (SQLi/XSS/traversal) → LIVE
  ✅ src/core/crawler.js        — BFS/DFS crawler, cheerio (Node) + DOMParser (browser) + regex fallback (5.6 KB)
                                  Discovers links, forms, query params, saves to SiteModel → WORKING
  ✅ src/core/httpClient.js     — Fetch adapter wrapper, Node + browser compatible (1.3 KB)
  ✅ src/core/injection.js      — SQLi / reflected XSS / path traversal FULLY WIRED to engine Phase 3 (6.9 KB)
  ✅ src/core/models.js         — All data models: Workspace, Project, Target, ScanJob,
                                  Finding, Evidence, AuditEvent, ModuleDef, ScanPolicy (5.7 KB)
  ✅ src/core/moduleRegistry.js — 12 registered ModuleDefs (6.9 KB):
                                  exposure.env.direct, exposure.env.variants,
                                  exposure.backup.db_dumps, exposure.backup.archives,
                                  misconfig.dirlisting.generic, vcs.git.exposed,
                                  debug.stacktraces, injection.sqli.basic,
                                  injection.xss.reflected_basic, injection.path_traversal.basic,
                                  tls.headers.basic
  ✅ src/core/policyRegistry.js — 3 scan policies fully wired (2.5 KB):
                                  policy_normal     — passive only
                                  policy_aggressive — passive + SQLi + XSS
                                  policy_extreme    — ALL modules enabled
  ✅ src/core/siteModel.js      — Per-target SiteModel: addEndpoint, getParamEndpoints,
                                  getAllEndpoints, dedup by URL key (2.5 KB)
  ✅ src/core/checks/tlsHeaders.js — TLS + security headers checker wired to engine Phase 1b (19.8 KB)

[PASSIVE EXPOSURE CHECKS - LIVE IN ENGINE]
  ✅ /.env direct exposure
  ✅ .env variants: .env.local, .env.backup, .env.bak, .env.old,
                   /script/.env, .env.prod, .env.staging, .env.development
  ✅ DB dump detection: /backup.sql, /db.sql, /dump.sql, /database.sql, /db_backup.sql
  ✅ Archive exposure: .zip, .tar.gz, site-backup variants
  ✅ Directory listing detection ("Index of /")
  ✅ .git component exposure: /.git/HEAD, /.git/config
  ✅ Verbose debug error / stack trace leakage
  ✅ TLS checker: plain HTTP detection, HTTPS redirect check
  ✅ Security headers: HSTS, CSP, X-Content-Type-Options, X-Frame-Options,
                       Permissions-Policy, Referrer-Policy

[ACTIVE INJECTION CHECKS - LIVE IN ENGINE]
  ✅ SQL injection (error-based) — 6 payloads, 12 error pattern signatures
  ✅ Reflected XSS — 4 payloads, raw reflection detection
  ✅ Path traversal / LFI — Linux + Windows payloads, OS file signature matching
  All injection checks: OWASP tags + CWE tags + evidence capture on every finding

[DORK ENGINE - WORKING]
  ✅ Google + GitHub dork generator (9.5 KB)
  ✅ Per-domain dork queries as clickable links
  ✅ Covers: filetype:env, intitle:index.of, filename:.env, db credentials, API keys

[REPORTING - PARTIALLY WORKING]
  ✅ backend/reportGenerator.js — HTML + Markdown report generator (11 KB) — BACKEND WORKS
  ❌ No UI button to trigger report generation yet
  ❌ No redacted report mode (strips actual secrets for client sharing)
  ❌ No PDF export — HTML/Markdown only
  ❌ No OWASP/CWE/CVE tagging in report output
  ❌ No executive summary auto-draft

[DATA PERSISTENCE - WORKING]
  ✅ backend/db.js — SQLite via better-sqlite3, auto-creates scanner.db
  ✅ Projects: create, get, list, update, delete (with cascade)
  ✅ Targets: create, bulk create, list by project, delete
  ✅ Findings: create, list by project (filterable), update status, severity summary,
               false positive marking, bulk upsert, get with evidence
  ✅ Evidence: create, list by finding
  ✅ AuditLog: write, recent(limit)
  ⚠️ backend/jobsStore.js — jobs are still IN-MEMORY only (not persisted to db.js)
  ⚠️ Jobs lost on server restart — findings are saved but job metadata is not

[SETUP]
  ✅ setup.sh — automated setup script for server/Termux environment


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SECTION 2: WHAT STILL NEEDS TO BE BUILT ❌
  (Verified missing by direct repo audit — June 5 2026)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[ENGINE MODULES - NOT BUILT]
  ❌ src/core/checks/cookieSession.js
      Cookie security flag checker: HttpOnly, Secure, SameSite flags
      Session ID entropy / URL exposure check
      Slot: engine.js Phase 1b (sibling of tlsHeaders.js)

  ❌ src/core/checks/jsSecretScan.js
      Fetch .js assets discovered by crawler
      Regex scan for: AKIA, sk-, AIza, Bearer tokens, Firebase config, Stripe keys
      Slot: engine.js Phase 2.5 (after crawl, before injection)

  ❌ src/core/checks/sourceMapDetect.js
      For every .js URL found by crawler, probe URL + '.map'
      Check if response is valid JSON with 'sources' key
      Exposes original unminified source code
      Slot: engine.js Phase 2.5 alongside jsSecretScan

  ❌ Advanced CVE pattern passive checks (add to engine.js + moduleRegistry.js)
      phpinfo.php / /debug / /_profiler endpoints
      /.svn/entries, /.hg/manifest (SVN/Mercurial metadata exposure)
      Dokans /script/.env (CVE-2025-70841 pattern)
      Laravel artisan + APP_KEY extraction (CVE-2024-55556 pattern)
      Vite dev server /@fs/ bypass (CVE-2025-46565 pattern)
      Mautic env exposure pattern (CVE-2024-47056 inspired)
      Moodle router directory listing (CVE-2025-62396 pattern)
      Cloud bucket misconfiguration: S3 / Azure open bucket detection

  ❌ injection.js additions needed:
      Command injection module (not in registry, not in engine)
      SSTI / template injection module (not started)
      File upload endpoint detection + test probes

  ❌ moduleRegistry.js additions needed:
      ModuleDef entries for all new checks above
      ModuleDef for cookie.session.flags
      ModuleDef for exposure.js.secrets
      ModuleDef for exposure.sourcemap
      ModuleDef for all CVE pattern modules
      ModuleDef for injection.cmdi.basic
      ModuleDef for injection.ssti.basic
      ModuleDef for injection.fileupload.detect

[DATA / JOBS - PARTIAL GAP]
  ⚠️ backend/jobsStore.js — jobs are in-memory only
      Jobs not written to db.js — lost on restart
      Fix: add jobs table to db.js + update jobsStore to persist

[UI VIEWS - NOT DONE / SKELETON ONLY]
  ❌ dashboardView.js — skeleton (633 B), needs real stats + charts from API
  ❌ Finding detail side-panel
      Evidence display (request/response snippets)
      Reproduction steps
      OWASP + CWE tag display
      Remediation guidance text
  ❌ Targets management view — no add/edit/delete target UI built
  ❌ Module catalog view — modules not browsable in UI
  ❌ Policy editor UI — policyRegistry.js exists, no UI to edit/select policies
  ❌ Report preview / export UI button — reportGenerator.js works, no trigger
  ❌ Workspace / project hierarchy view — flat project list only

[REPORTING - PARTIAL]
  ❌ UI button to trigger report generation
  ❌ OWASP/CWE/CVE tagging in report output
  ❌ Executive summary auto-draft
  ❌ PDF export (HTML/MD only right now)
  ❌ Redacted report mode (strips secrets for client sharing)

[ARCHITECTURE GAP - NEEDS RECONCILIATION]
  ⚠️ backend/engine-bridge.js (27 KB) duplicates scan logic from src/core/engine.js
      Two separate code trees doing similar work:
        /src/core/ = modular engine (engine.js, injection.js, crawler.js, registry)
        /backend/  = engine-bridge.js doing its own scan orchestration
      Fix: engine-bridge.js should import and delegate to src/core/engine.js
      src/core/ becomes single source of truth for all scan logic
      backend/ stays as HTTP API wrapper only

[INTEGRATIONS - NOT STARTED]
  ❌ ZAP connector (API/Docker wrapper)
  ❌ Metasploit RPC connector
  ❌ Nmap CLI output parser
  ❌ GitHub API integration for org/repo dorking automation
  ❌ Subdomain enumeration (crt.sh Certificate Transparency + DNS wordlist)
  ❌ API endpoint discovery (Swagger/OpenAPI spec at /api-docs, /swagger.json)

[SCAN SCHEDULING - NOT STARTED]
  ❌ Scheduled scans (daily/weekly/monthly)
  ❌ Scan timeline / calendar view
  ❌ Re-run / clone scan job UI
  ❌ Per-scan diff mode (compare current vs previous scan findings)

[CORRELATION ENGINE - NOT STARTED]
  ❌ Finding grouping / correlation logic
  ❌ Related findings linking (same secret reused across multiple endpoints)
  ❌ Severity escalation via correlation rules

[MULTI-WORKER ARCHITECTURE - NOT STARTED]
  ❌ Local agent mode (localhost:7777 worker)
  ❌ VPS worker agent API
  ❌ Android/Termux worker support
  ❌ Worker selector in UI ("Run on VPS / Local / Phone")

[AUTH / MULTI-USER - NOT STARTED]
  ❌ User authentication
  ❌ Role system (owner/analyst/view-only)
  ❌ Per-project multi-user access
  ❌ Client portal login

[MISC / POLISH - NOT DONE]
  ❌ WebSocket live job feed (currently polling-based)
  ❌ Mobile PWA mode (installable on Android/iOS)
  ❌ CLI client (scanner add-target, scanner run-scan)
  ❌ REST API documentation
  ❌ Screenshot capture on critical findings (headless browser)
  ❌ Notification system (Slack/Discord webhook on critical finding)
  ❌ Evidence Vault (encrypted storage, lock project at point-in-time)
  ❌ LLM-assisted finding summarization
  ❌ Electron/Tauri desktop wrapper
  ❌ Testing harness against DVWA/Mutillidae lab targets
  ❌ Module versioning / ruleset versioning
  ❌ Import/export of rulesets and policies
  ❌ Playbook templates per finding category
  ❌ Script snippet library panel (Python/Bash one-liners)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SECTION 3: IMMEDIATE PRIORITY BUILD ORDER 🔥
  (Ordered to minimize overlap risk and catch bugs early)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  STEP 1.  src/core/checks/cookieSession.js
           New file, zero overlap. Sibling of tlsHeaders.js.
           Add 1 import + 1 moduleEnabled() call to engine.js Phase 1b.
           Add ModuleDef to moduleRegistry.js.

  STEP 2.  src/core/checks/jsSecretScan.js
           New file. Reads .js URLs from SiteModel (crawler already saves them).
           Add to engine.js Phase 2.5 (after crawl, before injection).
           Add ModuleDef to moduleRegistry.js.

  STEP 3.  src/core/checks/sourceMapDetect.js
           New file. Sibling of jsSecretScan.js. Probes url+'.map' for each .js asset.
           Add to engine.js Phase 2.5 alongside jsSecretScan.
           Add ModuleDef to moduleRegistry.js.

  STEP 4.  CVE pattern passive checks in engine.js
           Add checkPhpinfo(), checkSvnHg(), checkDokanEnv(), checkLaravelArtisan(),
           checkViteBypass(), checkMauticEnv(), checkMoodleListing(), checkCloudBuckets()
           to runPassiveExposureChecks() in engine.js.
           Add corresponding ModuleDefs to moduleRegistry.js.
           Zero structural change to engine — pure additive.

  STEP 5.  Persist jobs in db.js
           Add jobs table to backend/db.js.
           Update backend/jobsStore.js to write/read from db instead of memory.
           Zero API changes — server.js routes unchanged.

  STEP 6.  Report generation UI button
           Wire frontend button to call GET /api/scans/:jobId/report.
           reportGenerator.js already works on backend.
           One button + one fetch call in frontend/app.js.

  STEP 7.  Finding detail side-panel (UI)
           Expand findingsListView.js to show evidence, OWASP tag,
           CWE tag, remediation text on row click.
           Data already available via GET /api/projects/:id/findings.

  STEP 8.  Dashboard stats wired to real data
           Replace dashboardView.js skeleton with real API calls.
           Findings severity summary, job count, project count all available in API.

  STEP 9.  engine-bridge.js → src/core/engine.js unification
           Refactor engine-bridge.js to delegate scan execution to src/core/engine.js.
           Eliminates logic duplication. Biggest structural change — do last.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SECTION 4: NEW IDEAS & FEATURES TO ADD 💡
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  IDEA-01: CVE Pattern Tag Engine
    Auto-label findings that match known CVE patterns: "Matches CVE-YYYY-XXXXX pattern"
    Huge value for client reports.

  IDEA-02: Risk Score Dashboard Widget
    Project risk score (0-100), visual ring/gauge
    Color: green=safe, yellow=moderate, red=critical, dark red=evacuate

  IDEA-03: Ghost Mode (Passive Recon Only)
    Dorks + passive reads only — zero probes sent to target
    100% stealth — useful before client authorization signed

  IDEA-04: Target Import from File
    Upload .txt / .csv with list of domains/IPs
    Auto-normalize and bulk add to project
    NOTE: /api/projects/:id/targets/bulk already exists on backend

  IDEA-05: Live Threat Intel Badge
    When .env exposure found, show: "Actively exploited by bots at scale"
    Reference GreyNoise / Shodan context

  IDEA-06: Scope Lock System
    Define scope per project: only scan *.example.com
    Crawler auto-rejects out-of-scope URLs

  IDEA-07: Screenshot Capture on Critical Findings
    Headless browser screenshot when critical finding found
    Auto-attach to report as evidence

  IDEA-08: Findings Kanban Board
    Open → Confirmed → Mitigated → False Positive
    Drag and drop cards per finding
    NOTE: status field + PUT /api/findings/:id/status already exists

  IDEA-09: Per-Scan Diff Mode
    Compare current vs previous scan: NEW (red), FIXED (green), UNCHANGED (grey)
    Critical for recurring client assessments

  IDEA-10: Dork Assistant Panel
    In-app panel: type target type → get top 10 dork queries
    Stack-fingerprint aware

  IDEA-11: Mobile PWA Mode
    Installable on Android/iOS, offline-capable for viewing saved findings

  IDEA-12: Header Fingerprint Database
    Mini DB of server/framework response header signatures
    Auto-detect: Laravel, WordPress, Django, Rails, ASP.NET, Spring Boot

  IDEA-13: WebSocket Live Job Feed
    Replace polling with true real-time WebSocket log stream

  IDEA-14: Quick-Launch Scan Templates
    Pre-built configs: "WordPress Full Audit", "API Backend Check", "Laravel Env Hunt"

  IDEA-15: OWASP Top 10 2025 Coverage Checklist
    Visual checklist per project: green=covered, red=not tested
    Great for client deliverables

  IDEA-16: Subdomain Enumeration Module
    crt.sh Certificate Transparency logs (passive)
    DNS wordlist brute-force (top 500 subdomains)
    Auto-add discovered subdomains as new targets

  IDEA-17: API Endpoint Discovery
    Probe /api-docs, /swagger.json, /openapi.yaml
    Auto-import all endpoints as scan targets

  IDEA-18: Custom Payload Library Manager
    UI to add/edit/remove payloads per module
    Import/export compatible with SecLists format

  IDEA-19: Notification System
    Slack/Discord webhook on critical finding
    Email alert support

  IDEA-20: Evidence Vault
    Encrypted local storage for full HTTP request/response evidence
    Lock project at point-in-time for legal reporting


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SECTION 5: ARCHITECTURE NOTES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  TWO PARALLEL CODE TREES — NEEDS RECONCILIATION:

    /src/core/     = newer modular engine system
                     engine.js, injection.js, crawler.js, siteModel.js,
                     moduleRegistry.js, policyRegistry.js, checks/
                     → This is the CORRECT architecture going forward

    /backend/      = Express HTTP API + engine-bridge.js doing its own scan logic
                     engine-bridge.js (27 KB) currently duplicates orchestration
                     that already exists in src/core/engine.js

  RECOMMENDED UNIFICATION PATH:
    1. engine-bridge.js imports runScanJob from src/core/engine.js
    2. engine-bridge.js becomes a thin adapter (format input, relay output to db.js)
    3. src/core/ is single source of truth for all scan logic
    4. backend/ stays as HTTP API + DB layer only
    NOTE: Do this LAST (Step 9) — it’s the biggest structural change

  CORS / DEPLOYMENT STRATEGY:
    Browser-only scanning is limited by CORS sandbox
    Recommended: local Node agent on port 7777 OR full VPS backend
    engine.js + httpClient.js adapter design already supports both modes

  DB FILE LOCATION:
    backend/data/scanner.db (auto-created by better-sqlite3 on first run)
    backend/data/ exists in repo as empty placeholder (.gitkeep)
    .gitignore should exclude scanner.db from commits (verify this is set)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  END OF DEV TRACKER — web-vuln-console
  Always be scanning. Always authorized. Always document.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
