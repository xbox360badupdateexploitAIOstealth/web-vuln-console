================================================================================
  WEB-VULN-CONSOLE — DEV TRACKER v3 (FULLY SOURCE-VERIFIED)
  Project: Web Vulnerability Scanner Console
  Repo: xbox360badupdateexploitAIOstealth/web-vuln-console
  Last Updated: June 6, 2026
  Status: Active Development — Phase 2/3

  AUDIT METHOD: Every file listed below was directly read (not guessed).
  moduleRegistry.js, engine.js, injection.js, server.js, and all view
  files were read line-by-line. Previous dev.txt had several inaccuracies
  corrected here.
================================================================================

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SECTION 1: VERIFIED FILE INVENTORY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[ROOT]
  ✅ .gitignore          (275 B)
  ✅ README.md           (139 B)
  ✅ setup.sh            (4.8 KB)
  ✅ dev.txt             (this file)

[FRONTEND — frontend/]
  ✅ frontend/index.html     (9.5 KB)  — dark terminal console UI shell
  ✅ frontend/style.css      (14 KB)   — cyberpunk terminal theme, responsive
  ✅ frontend/loader.css     (1.2 KB)  — loading animations
  ✅ frontend/app.js         (~50 KB)  — core SPA frontend logic (LARGEST FILE)
  ✅ frontend/report.js      — report generation UI module (size unverified)

[PUBLIC — public/]
  ✅ public/index.html   — entry point / redirect
  ✅ public/styles.css   — base styles

[BACKEND — backend/]
  ✅ backend/server.js           (16.1 KB)  — Express HTTP API v2.0.0 (SOURCE VERIFIED)
  ✅ backend/config.js           (715 B)
  ✅ backend/db.js               (17.5 KB)  — SQLite via better-sqlite3
  ✅ backend/jobsStore.js        (1.7 KB)   — ⚠️ IN-MEMORY ONLY (not persisted)
  ✅ backend/worker.js           (2.2 KB)   — background job worker
  ✅ backend/engine-bridge.js    (44.1 KB)  — standalone scan engine (NOT delegating to src/core yet)
  ✅ backend/dorkEngine.js       (9.5 KB)   — Google + GitHub dork generator
  ✅ backend/reportGenerator.js  (11.1 KB)  — HTML + Markdown report builder
  ✅ backend/package.json        (484 B)
  ✅ backend/README.md           (1.4 KB)
  ✅ backend/data/               — empty placeholder dir for scanner.db
  ✅ backend/middleware/rateLimiter.js    (1.1 KB)
  ✅ backend/middleware/requestLogger.js (618 B)
  ✅ backend/routes/dorksRoute.js        (430 B)  — GET /api/dorks
  ✅ backend/routes/reportsRoute.js      (872 B)  — GET /api/scans/:jobId/report
  ✅ backend/utils/normalize.js          (1.3 KB)
  ✅ backend/utils/retry.js              (1.0 KB)
  ✅ backend/utils/severityScore.js      (1.4 KB)

[SRC/CORE — src/core/]
  ✅ src/core/engine.js          (18.8 KB)  — SOURCE VERIFIED
  ✅ src/core/crawler.js         (9.5 KB)
  ✅ src/core/httpClient.js      (1.3 KB)
  ✅ src/core/injection.js       (12.6 KB)  — SOURCE VERIFIED
  ✅ src/core/models.js          (5.7 KB)
  ✅ src/core/moduleRegistry.js  (8.9 KB)   — SOURCE VERIFIED
  ✅ src/core/policyRegistry.js  (3.2 KB)
  ✅ src/core/siteModel.js       (2.5 KB)
  ✅ src/core/checks/tlsHeaders.js     (19.8 KB) — TLS + security headers checker
  ❌ src/core/checks/cookieSession.js  — DOES NOT EXIST
  ❌ src/core/checks/jsSecretScan.js   — DOES NOT EXIST
  ❌ src/core/checks/sourceMapDetect.js — DOES NOT EXIST

[SRC/UI — src/ui/]
  ✅ src/ui/app.js      (1.4 KB)
  ✅ src/ui/state.js    (239 B)
  ✅ src/ui/tos.js      (3.2 KB)
  ✅ src/ui/views/findingsListView.js  (5.1 KB)  — SOURCE VERIFIED: real table w/ status/severity
  ✅ src/ui/views/jobConsoleView.js    (3.2 KB)  — SOURCE VERIFIED: real live log view
  ✅ src/ui/views/projectListView.js   (1.7 KB)  — SOURCE VERIFIED: real project list
  ⚠️ src/ui/views/dashboardView.js    (633 B)   — SOURCE VERIFIED: SKELETON ONLY — no real data
  ❌ src/ui/views/targetsView.js       — DOES NOT EXIST
  ❌ src/ui/views/policyView.js        — DOES NOT EXIST
  ❌ src/ui/views/settingsView.js      — DOES NOT EXIST (was in original design spec)
  ❌ src/ui/views/modulesView.js       — DOES NOT EXIST (was in original design spec)
  ❌ src/ui/views/projectDetailView.js — DOES NOT EXIST (was in original design spec)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SECTION 2: VERIFIED API ROUTES (server.js read line-by-line)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ✅ GET    /api/health
  ✅ GET    /api/stats               — global totals: projects/targets/jobs/findings+severity
  ✅ GET    /api/projects
  ✅ POST   /api/projects
  ✅ GET    /api/projects/:id
  ✅ PUT    /api/projects/:id
  ✅ PATCH  /api/projects/:id        — partial update
  ✅ DELETE /api/projects/:id
  ✅ GET    /api/projects/:id/targets
  ✅ POST   /api/projects/:id/targets
  ✅ POST   /api/projects/:id/targets/bulk
  ✅ DELETE /api/targets/:targetId
  ✅ POST   /api/scans               — queue scan (policyId, targets[])
  ✅ GET    /api/scans               — list all jobs (?projectId filter)
  ✅ GET    /api/scans/:jobId
  ✅ GET    /api/scans/:jobId/results
  ✅ POST   /api/scans/:jobId/cancel
  ✅ POST   /api/scans/:jobId/retry  — re-queues failed/canceled job
  ✅ GET    /api/projects/:id/findings   — ?severity ?status ?category ?limit ?offset
  ✅ PUT    /api/findings/:id/status
  ✅ GET    /api/audit
  ✅ GET    /api/dorks               — via dorksRoute
  ✅ GET    /api/scans/:jobId/report — via reportsRoute


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SECTION 3: VERIFIED MODULE REGISTRY (moduleRegistry.js read line-by-line)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  CONFIRMED 11 ModuleDefs (NOT 12 as old dev.txt claimed):

  PASSIVE — EXPOSURE:
  ✅ exposure.env.direct          — /.env exposure, dotenv detection
  ✅ exposure.env.variants        — 8 variant paths (.env.local/.backup/.bak/etc.)
  ✅ exposure.backup.db_dumps     — /backup.sql, /db.sql, /dump.sql, etc.
  ✅ exposure.backup.archives     — .zip, .tar.gz backup archives
  ✅ misconfig.dirlisting.generic — "Index of /" detection
  ✅ vcs.git.exposed              — /.git/HEAD, /.git/config
  ✅ debug.stacktraces            — stack trace / verbose error leakage

  ACTIVE — INJECTION:
  ✅ injection.sqli.basic         — 6 payloads, 12 DB error patterns
  ✅ injection.xss.reflected_basic — 4 payloads, raw reflection check
  ✅ injection.path_traversal.basic — Linux + Windows traversal, file sigs

  PASSIVE — TLS:
  ✅ tls.headers.basic            — HSTS/CSP/X-Content-Type/X-Frame/Perms/Referrer

  NOT IN REGISTRY (engine-bridge.js has these but src/core/ does NOT):
  ❌ cookie.session.flags
  ❌ exposure.js.secrets
  ❌ exposure.sourcemap
  ❌ exposure.cve.cpanel_whm         — NEW TODO
  ❌ exposure.cve.laravel_env_hunt   — NEW TODO
  ❌ injection.cmdi.basic
  ❌ injection.ssti.basic
  ❌ injection.fileupload.detect
  ❌ CVE pattern modules (phpinfo, SVN, Dokan, Vite, Mautic, Moodle, S3)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SECTION 4: VERIFIED ENGINE WIRING (engine.js read line-by-line)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  engine.js v1.2.0 phases:

  Phase 1  — runPassiveExposureChecks()
    Wired: exposure.env.direct, exposure.env.variants, exposure.backup.db_dumps,
           exposure.backup.archives, misconfig.dirlisting.generic,
           vcs.git.exposed, debug.stacktraces

  Phase 1b — runTlsHeaderChecks()  (imports from checks/tlsHeaders.js)
    Wired: tls.headers.basic
    ⚠️ cookieSession NOT WIRED (file doesn't exist yet)

  Phase 2  — crawlTargetAndBuildSiteModel()  (imports from crawler.js)
    maxDepth: 2, maxPages: 30
    Builds SiteModel with endpoints and parameterized URLs
    ⚠️ Phase 2.5 NOT WIRED: jsSecretScan, sourceMapDetect not implemented

  Phase 3  — runActiveInjectionChecks()  (imports from injection.js)
    Wired: injection.sqli.basic, injection.xss.reflected_basic,
           injection.path_traversal.basic
    Probe cap: 100 probes per target
    ⚠️ cmdi, SSTI, fileupload NOT in injection.js


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SECTION 5: VERIFIED INJECTION.JS (injection.js read line-by-line)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  CONFIRMED functions in injection.js:
  ✅ runActiveInjectionChecks()     — main entry, 100-probe cap
  ✅ runSqliOnEndpoint()            — 6 payloads, 12 DB error signatures
  ✅ runXssOnEndpoint()             — 4 payloads, raw body reflection check
  ✅ runPathTraversal()             — Linux + Windows payloads + OS file sigs

  NOT IN injection.js:
  ❌ command injection
  ❌ SSTI / template injection
  ❌ file upload detection


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SECTION 6: KNOWN GAPS & WARNINGS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ⚠️ OLD DEV.TXT CLAIMED 12 MODULES — ACTUAL COUNT IS 11
     Old file listed 12 ModuleDefs. Source read confirms 11.
     Missing from the list was a phantom entry — no 12th ModuleDef exists.

  ⚠️ TWO PARALLEL SCAN ENGINES (critical architecture gap)
     backend/engine-bridge.js (44 KB) = its own complete scan system
     src/core/engine.js (18.8 KB)     = modular engine (correct architecture)
     They are completely independent. engine-bridge.js does NOT import engine.js.
     Fix: engine-bridge.js should import runScanJob from src/core/engine.js
     and become a thin HTTP/DB adapter only.
     STATUS: NOT FIXED (TODO-12, do last)

  ⚠️ JOBS NOT PERSISTED
     backend/jobsStore.js is pure in-memory Map.
     Server restart = all job metadata lost.
     Findings ARE saved to db.js (SQLite) but job records are not.
     Fix: add jobs table to db.js, update jobsStore to persist.

  ⚠️ dashboardView.js IS A 633-BYTE SKELETON
     Source verified: zero API calls, no real data.
     GET /api/stats exists and works. Just needs wiring.

  ⚠️ 5 UI VIEWS MISSING ENTIRELY
     targetsView.js, policyView.js, settingsView.js,
     modulesView.js, projectDetailView.js — none exist in repo.
     All were in the original design spec (scanner.md).

  ⚠️ frontend/report.js EXISTS but integration unverified
     File present in frontend/ but not source-read.
     No evidence it is imported in app.js yet.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SECTION 7: PRIORITY TODO LIST — ORDERED (June 6 2026)
  Ordering rationale: new isolated files first (zero breakage risk),
  then edits, then DB/backend, then UI, then big structural changes last.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ── TIER 1: NEW ENGINE CHECK FILES (src/core/checks/) ──────────────────────
  Zero overlap with existing code. Each is a new isolated file + small
  edits to engine.js + moduleRegistry.js.

  TODO-01  src/core/checks/cookieSession.js  [NEW FILE]
           Cookie security checker: HttpOnly, Secure, SameSite, SameSite=None+Secure
           Session ID entropy + URL-exposed session token check
           Wire: engine.js Phase 1b alongside tlsHeaders
           Add: ModuleDef  cookie.session.flags  to moduleRegistry.js
           Touches: new file, engine.js (2 lines), moduleRegistry.js (1 entry)

  TODO-02  src/core/checks/jsSecretScan.js  [NEW FILE]
           Fetch every .js asset URL from SiteModel
           Regex scan for: AKIA (AWS key), sk- (Stripe), AIza (Google), Bearer tokens,
           Firebase config objects, GitHub tokens, JWT strings
           Wire: engine.js Phase 2.5 (after crawl, before Phase 3)
           Add: ModuleDef  exposure.js.secrets  to moduleRegistry.js
           Touches: new file, engine.js (2 lines), moduleRegistry.js (1 entry)

  TODO-03  src/core/checks/sourceMapDetect.js  [NEW FILE]
           For every .js URL in SiteModel, probe url + '.map'
           If response is valid JSON with 'sources' key → flag as critical
           Exposes original unminified source code and file paths
           Wire: engine.js Phase 2.5 alongside jsSecretScan
           Add: ModuleDef  exposure.sourcemap  to moduleRegistry.js
           Touches: new file, engine.js (2 lines), moduleRegistry.js (1 entry)

  ── TIER 2: CVE PATTERN CHECKS + IP RANGE MODULES ──────────────────────────

  TODO-04  CVE-2026-41940 — cPanel & WHM IP Range Scanner  [NEW FILE or engine edit]
           New module: exposure.cve.cpanel_whm
           For IP range input (CIDR or list): probe each IP for cPanel/WHM
           fingerprints on ports 2082, 2083, 2086, 2087, 8080, 8443:
             - Check /cpanel, /whm, /cpsess, and cPanel login page patterns
             - Check X-cPanel-Version response header
             - Flag CVE-2026-41940 pattern (unauthenticated info disclosure
               or session token exposure depending on advisory)
           This is an IP RANGE scan — needs special input mode in UI
           Severity: critical if confirmed cPanel+WHM on exposed port
           Add: ModuleDef  exposure.cve.cpanel_whm  to moduleRegistry.js
           Add: policyRegistry.js update (policy_extreme should enable)
           Touches: new check file, engine.js Phase 1 entry, moduleRegistry.js
           ⚠️ NOTE: advisory details for CVE-2026-41940 may still be under embargo;
              check NVD/SentinelOne before finalizing detection signatures

  TODO-05  Misconfigured Laravel .env Hunt  [NEW FILE or extend existing]
           New module: exposure.cve.laravel_env_hunt
           Aggressive Laravel-specific .env grabber, goes beyond exposure.env.direct:
             - Probe standard paths: /.env, /public/.env, /storage/.env
             - Probe misconfig patterns where public/ is NOT the doc root:
               /app/.env, /laravel/.env, /backend/.env, /api/.env
             - Probe common backup clones: /.env.bak, /.env~, /.env.save
             - /script/.env  (Dokan/multi-tenant Laravel — CVE-2025-70841)
             - On any 200 hit: regex-extract APP_KEY, DB_PASSWORD, DB_HOST,
               REDIS_URL, MAIL_PASSWORD, AWS_SECRET_ACCESS_KEY
             - If APP_KEY found: flag as critical + annotate Laravel RCE risk
               (CVE-2024-55556 pattern — APP_KEY → deserialization RCE in
               vulnerable Laravel apps like Crater Invoice)
           Severity: critical on APP_KEY or cloud creds; high on any .env hit
           Add: ModuleDef  exposure.cve.laravel_env_hunt  to moduleRegistry.js
           Touches: new file, engine.js, moduleRegistry.js, policyRegistry.js

  TODO-06  Additional CVE passive checks  [EDIT engine.js + moduleRegistry.js]
           Add to Phase 1 runPassiveExposureChecks():
             checkPhpinfo()         — /phpinfo.php, /info.php, /debug, /_profiler
             checkSvnHg()           — /.svn/entries, /.hg/manifest
             checkViteBypass()      — /@fs/ path bypass, open dev server check
                                      (CVE-2025-46565 pattern)
             checkMauticEnv()       — Mautic .env web-reachable pattern
                                      (CVE-2024-47056 inspired)
             checkMoodleListing()   — Moodle r.php router dir listing
                                      (CVE-2025-62396 pattern)
             checkCloudBuckets()    — S3/Azure open bucket probe
             checkWordPressDebug()  — /wp-config.php.bak, debug.log in /wp-content/
           Add ModuleDefs for each above
           Touches: engine.js, moduleRegistry.js

  ── TIER 3: INJECTION.JS ADDITIONS ─────────────────────────────────────────

  TODO-07  injection.js additions  [EDIT src/core/injection.js]
           Add runCommandInjection():
             OS command injection via form params
             Payloads: ; id, & whoami, | id, `id`, $(id)
             Detection: response contains uid= / root / windows username patterns
           Add runSstiChecks():
             Template injection: {{7*7}}, ${7*7}, #{7*7}, <%= 7*7 %>
             Detection: response contains 49 or error mentioning template engine
           Add runFileUploadDetect():
             Detect file upload forms from SiteModel
             Probe with: benign .txt upload, then .php/.jsp test extension
             Detection: 200 response + filename reflected, or error mentioning extension
           Add ModuleDefs: injection.cmdi.basic, injection.ssti.basic,
                           injection.fileupload.detect
           Update policyRegistry.js: policy_extreme enables all three
           Touches: injection.js, moduleRegistry.js, policyRegistry.js

  ── TIER 4: BACKEND FIXES ───────────────────────────────────────────────────

  TODO-08  Persist jobs to DB  [EDIT backend/db.js + backend/jobsStore.js]
           Add jobs table to db.js:
             id, project_id, status, policy_id, targets_json, description,
             created_at, started_at, finished_at, result_json
           Update jobsStore.js:
             createJob / updateJob / getJob / listJobs all read/write SQLite
             saveJobResult writes result_json column
           Zero API changes — server.js routes unchanged
           Fixes: job metadata lost on server restart
           Touches: db.js, jobsStore.js

  ── TIER 5: FRONTEND / UI ───────────────────────────────────────────────────

  TODO-09  Dashboard wired to real data  [EDIT src/ui/views/dashboardView.js]
           Replace 633-byte skeleton with full view:
           GET /api/stats → project count, target count, job counts,
           findings severity breakdown ring chart
           Recent jobs table + recent critical/high findings list
           Touches: dashboardView.js only

  TODO-10  Report generation UI  [EDIT frontend/app.js + verify report.js]
           Wire "Generate Report" button → GET /api/scans/:jobId/report
           Show download link for HTML and Markdown outputs
           Verify frontend/report.js is actually imported in app.js
           Touches: frontend/app.js, report.js

  TODO-11  Finding detail side-panel  [EDIT src/ui/views/findingsListView.js]
           On row click: slide-in panel showing:
             Full URL + HTTP method + status code
             Request/response evidence snippet
             OWASP tag + CWE tag
             Remediation guidance text
             Status changer: open → confirmed → mitigated → false_positive
           Data: GET /api/projects/:id/findings already returns evidence
           Touches: findingsListView.js

  TODO-12  Targets management view  [NEW FILE src/ui/views/targetsView.js]
           Per-project: targets list with add / edit / delete UI
           Bulk paste import (line-separated domains)
           Env tag (prod/staging/dev)
           IP range input field (feeds into cPanel/WHM scan — TODO-04)
           API: POST/DELETE /api/projects/:id/targets already exists
           Touches: new file + app.js routing

  TODO-13  Policy editor UI  [NEW FILE src/ui/views/policyView.js]
           Show all scan policies (normal/aggressive/extreme)
           Module toggle checkboxes per policy
           policyRegistry.js already has the data model
           Touches: new file + app.js routing

  ── TIER 6: ARCHITECTURE (DO LAST) ─────────────────────────────────────────

  TODO-14  engine-bridge.js → src/core/engine.js unification
           engine-bridge.js (44 KB) currently has its own full scan orchestration
           independent of src/core/.
           It should:
             import { runScanJob } from '../src/core/engine.js'
             normalize backend job input → call runScanJob → persist to db.js
           engine-bridge.js becomes a thin adapter (~3 KB)
           src/core/ becomes single source of truth for ALL scan logic
           backend/ becomes HTTP API + DB wrapper only
           ⚠️ BIGGEST STRUCTURAL CHANGE — requires careful testing
           Do after all Tier 1-5 items are complete and src/core/ is feature-complete
           Touches: engine-bridge.js (near-full rewrite), possibly engine.js exports

  ── TIER 7: NICE-TO-HAVE ────────────────────────────────────────────────────

  TODO-15  WebSocket live job feed (replace polling)
  TODO-16  Scan diff mode (new vs previous: NEW/FIXED/UNCHANGED per finding)
  TODO-17  Subdomain enumeration (crt.sh Certificate Transparency + DNS wordlist)
  TODO-18  Scheduled scans (daily/weekly cron)
  TODO-19  Slack/Discord webhook on critical finding
  TODO-20  Screenshot capture on critical findings (headless browser)
  TODO-21  CLI client (scanner add-target, scanner run-scan)
  TODO-22  Mobile PWA (installable Android/iOS)
  TODO-23  LLM-assisted finding summarization
  TODO-24  User auth + role system (owner/analyst/view-only)
  TODO-25  Evidence Vault (encrypted storage, project lock)
  TODO-26  Custom payload library manager (import SecLists format)
  TODO-27  OWASP Top 10 2025 coverage checklist per project
  TODO-28  Target import from .txt/.csv file
  TODO-29  Quick-launch scan templates (WordPress Full Audit, Laravel Env Hunt,
           cPanel Range Sweep, Vite Dev Server Hunt)
  TODO-30  Dork assistant panel (in-app clickable dorks)
  TODO-31  REST API documentation page
  TODO-32  settingsView.js — API keys, scan defaults, concurrency settings
  TODO-33  modulesView.js — browsable module catalog
  TODO-34  projectDetailView.js — single project full summary view
  TODO-35  Per-project "playbook" mode (sequence of scan types saved as template)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SECTION 8: ARCHITECTURE NOTES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  TWO PARALLEL SCAN CODE TREES (must be reconciled — TODO-14):

    /src/core/     — modular engine (CORRECT architecture going forward)
                     engine.js, injection.js, crawler.js, siteModel.js,
                     moduleRegistry.js (11 ModuleDefs), policyRegistry.js, checks/

    /backend/      — Express API + engine-bridge.js (its own scan logic)
                     engine-bridge.js (44 KB) runs scans independently.
                     Does NOT import src/core/engine.js at all.

  SCAN POLICY IDs (policyRegistry.js — 3 policies):
    policy_normal     — passive only
    policy_aggressive — passive + SQLi + XSS
    policy_extreme    — ALL modules (will grow as Tier 1-3 TODOs complete)

  DB FILE:
    backend/data/scanner.db (auto-created by better-sqlite3 on first run)
    .gitignore excludes scanner.db from commits

  DEPLOYMENT:
    Node backend port 7777 (Termux / VPS)
    Frontend served as static files by Express from /frontend
    Browser-only mode: limited by CORS — backend agent recommended for real scans

  IP RANGE INPUT (needed for TODO-04 — cPanel/WHM scanner):
    Targets currently take a single host string.
    IP range scanning needs CIDR parsing (e.g., 192.168.1.0/24 → list of IPs)
    Add a CIDR expander utility in backend/utils/cidrExpand.js
    Wire into bulk target creation OR as a separate scan type in engine


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  END — dev.txt v3 — web-vuln-console
  Source-verified June 6 2026. Always authorized. Always documented.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
